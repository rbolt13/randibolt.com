---
title: "Phone Data"
date: "2025-02-06"
categories: [news, code, analysis]
image: "image.jpg"
---

This is a post with executable code.

```{r}
# install.packages("tidyverse")
```

```{r}
library("tidyverse")
```

# Load Data

```{r}
ccd <- read_csv("Call Center Data.csv")
```

# Look at the data

```{r}
head(ccd)
tail(ccd)
hist(ccd$`Incoming Calls`)
```

